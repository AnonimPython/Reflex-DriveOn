import reflex as rx
from ..ui.colors import *

from ..ui.admin_links import admin_links


def admin_support() -> rx.Component:
    return rx.box(
        rx.hstack(
            
            
        ),
        background_color=ADMIN_BACKGROUND,
        height="100vh",
    )